import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF121212); // Deep premium grey
  static const Color surface = Color(0xFF1E1E1E);    // Card surface
  static const Color iconContainer = Color(0xFF2C2C2C); // Transaction icon bg

  // Accents
  static const Color primary = Color(0xFFFF5F2D);    // Vibrant Orange
  static const Color primaryLight = Color(0xFFFF8A5C); // Gradient end

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF888888);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
}
